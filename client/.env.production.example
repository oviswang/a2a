# Copy this file to `client/.env.production`, replace the host with your real API, commit.
# Vite loads it on every `vite build` / Vercel build — no Vercel dashboard URL vars needed.
#
# VITE_SERVER_URL=https://your-game-server.example.com
