# API service image only uses root workspace + shared + server (see server/Dockerfile).
# Client ships on Vercel — omitting it shrinks Railway snapshots/CLI uploads (~40MB+).
client/

.vercel/
.cursor/
